/** @format */

import { Document, Page, Text, View, StyleSheet } from "@react-pdf/renderer";

const PdfViewer = () => {
  const styles = StyleSheet.create({
    page: {
      flexDirection: "row",
      backgroundColor: "#E4E4E4",
    },
    section: {
      margin: 10,
      padding: 10,
      flexGrow: 1,
    },
  });
  return <div>dsadasdsad</div>;
};

export default PdfViewer;
